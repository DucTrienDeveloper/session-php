#include<iostream>
#include<cmath>
using namespace std;

int main() {
	int a;
	cout << "nhap so luong mua ao ";
	cin >> a;
	if (a >= 5 && a <= 10) {
		cout << "thanh toan la : ";
		cout << a * 12 - (a * 0.1);
	}
	if (a >= 11 && a <= 20) {
		cout << "so tien thanh toan : ";
		cout << a * 12 - (a * 0.15);
	}
	if (a > 21 && a <= 30) {
		cout << "so tien thanh toan : ";
		cout << a * 12 - (a * 0.2);

	}
	if (a >= 31) {
		cout << "so tien thanh toan : ";
		cout << a * 31 - (a * 0.25);
	}
}	